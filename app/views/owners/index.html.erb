<h1>Index Owners</h1>

<ul>
  <% @owners.each do |owner|  %>
    <li>
      <%= link_to owner.name, owner_path(owner) %> - <%= link_to "Edit", edit_owner_path(owner) %>
    </li>
    <% end %>
</ul>
<%= link_to "Create new owner!", new_owner_path %>
